import React from 'react'
// I chose to make this a Stateless Functional Component since it is
// just rendering some UI

const Header = (props) => (
	<div className="list-books-title">
		<h1>MyReads</h1>
	</div>
)

export default Header
